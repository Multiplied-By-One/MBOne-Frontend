import { Button } from '@material-ui/core';
import {withStyles} from '@material-ui/styles';

const SystemMapButton = withStyles({
  root: {
    borderRadius: 50,
    height: 100,
    fontFamily: "Walter Turncoat"
  }
})(Button)
 
export default SystemMapButton